# 🚀 خطة أوفرهول شاملة — NEON CLASH: OVERDRIVE
### (استضافة على Render + تثبيت كتطبيق + تحسين تحكم وعرض الهاتف)

> **هدف الخطة:** تحويل اللعبة من مشروع محلي (Windows + PowerShell) إلى لعبة ويب منشورة وقابلة للتثبيت على أي هاتف خلال 2–3 أسابيع عمل.

---

## 🔍 أولاً: جرد الوضع الحالي (Audit)

| البند | الوضع الحالي | المطلوب |
|---|---|---|
| نوع المشروع | ملفات ثابتة: `index.html` + `css/` + `js/` بدون نظام بناء (بدون npm) | ✅ ممتاز للاستضافة — لا حاجة لبناء |
| الشبكة | PeerJS عبر السحابة المجانية `0.peerjs.com` + STUN (يعمل عبر الإنترنت فعلاً، وليس LAN فقط) | إبقاؤه + خيار استضافة PeerServer خاص لاحقاً |
| تحكم اللمس | عصتا تحكم ديناميكيتان + 7 أزرار لمس موجودة (`mobile-controls-layer`) | تحسين الاستجابة والتخطيط والحجم |
| استجابة CSS | Media queries عند 480/560/768/900px + portrait mode | فجوات: safe-areas, 100dvh, rotate overlay |
| الدقة | `resolutionScale` + DPR scaling موجودان في `Renderer.js` | ربطهما بمنظم FPS تلقائي |
| التشغيل المحلي | `Play_Game.bat` → `serve.ps1` (ويندوز فقط) | يبقى كما هو (لا كسر للتوافق) |
| الإعدادات | قائمة شاملة (حساسية، صوت، اهتزاز) | إضافة خيارات تحكم لمس جديدة |
| PWA | ❌ لا يوجد `manifest` ولا `Service Worker` | الملفان الأساسيان للتحميل والأوفلاين |
| النشر | ❌ لا يوجد `render.yaml` / خادم / Dockerfile | إضافة خيارَي نشر (Static / Web Service) |
| QR Code | ✅ موجود في اللوبي لمشاركة غرفة اللعب | سيُستخدم لمشاركة رابط Render بعد النشر |

---

## 🧭 المرحلة 1 — الاستضافة على Render (أولوية قصوى — سهل وسريع)

اللعبة ثابتة 100%، لذا النشر مباشر جداً. **خياران:**

### الخيار A (موصى به للبدء): Static Site مجاني على Render
- الخطة المجانية للـ Static Sites **لا تنام** أبداً ولا تحتاج بطاقة ائتمانية.
- Render يمنح HTTPS تلقائياً — وهذا **شرط أساسي** لأن WebRTC وService Worker لا يعملان إلا عبر HTTPS.

**الخطوات:**
1. إضافة ملف `render.yaml` في جذر المستودع:
   ```yaml
   services:
     - type: web
       name: neon-clash
       runtime: static
       staticPublishPath: .
       routes:
         - type: rewrite
           source: /*
           destination: /index.html
       headers:
         - path: /js
           name: Cache-Control
           value: public, max-age=604800
   ```
2. في لوحة Render: **New → Static Site** → ربط المستودع → Build Command فارغ → Publish Directory = `.`
3. سرّ النشر: < دقيقتان. يصبح الرابط: `https://neon-clash-xxxx.onrender.com`
4. سيتولّى QR code الموجود مسبقاً مشاركة رابط الغرفة مع الهواتف.

### الخيار B (احتياطي / للمستقبل): Web Service بخادم Node صغير
- مفيد فقط إذا أردنا لاحقاً: استضافة PeerServer خاص، أو API للترتيب العالمي، أو WebSocket للانضمام بدلاً من P2P.
- خادم `server.js` بـ Node المدمج (بدون أي مكتبات خارجية — بدون npm install).
- ⚠️ ملاحظة: خطط Render المجانية للـ Web Services **تنام بعد 15 دقيقة خمول**؛ أول زيارة تستغرق ~50 ثانية للاستيقاظ. لذلك نبدأ بـ Static.

### التحقق المتوقع بعد المرحلة 1
- [ ] فتح الرابط من الهاتف والكمبيوتر على شبكتين مختلفتين (4G + Wi-Fi).
- [ ] إنشاء غرفة والانضمام إليها من جهازين عبر رمز الغرفة (PeerJS cloud يعمل عبر HTTPS).
- [ ] Lighthouse: أداء > 90 على المحاكاة المحمولة.

---

## 📲 المرحلة 2 — جعل اللعبة قابلة للتحميل (PWA = أفضل حل)

تثبيت اللعبة من المتصفح مباشرة على شاشة الهاتف الرئيسية، **بدون متجر تطبيقات**، وتعمل **أوفلاين** (وضع البوتات).

### الملفات المطلوبة
1. **`manifest.webmanifest`** — اسم اللعبة، لون الخلفية النيون، اتجاه الشاشة `landscape`، أيقونات.
2. **الأيقونات** — `icons/icon-192.png` و`icons/icon-512.png` + أيقونة iOS (`apple-touch-icon`).
3. **`service-worker.js`** — استراتيجية **Cache-First** لملف اللعبة كلها (~700KB):
   - يخزّن: `index.html`, `css/style.css`, كل `js/**` بما فيها `peerjs.min.js`.
   - إصدار كاش `neon-clash-v1` يتحدّث مع كل تحديث (حتى لا يعلق اللاعبون بنسخة قديمة).
   - نمط **Network-First** لأي ملفات متغيرة مستقبلاً.
4. **تسجيل الـ SW** في `index.html` + زر «⬇ تثبيت اللعبة» في اللوبي:
   - حدث `beforeinstallprompt` → إظهار زر التثبيت المخصص (كروم/سامسونج).
   - على iOS: رسالة «Share → Add to Home Screen» مع شرح مصوّر صغير (Safari لا يعرض زر تثبيت تلقائياً).
5. **وسوم iOS** في `<head>`:
   ```html
   <meta name="apple-mobile-web-app-capable" content="yes">
   <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
   <meta name="theme-color" content="#06080e">
   ```

### النتيجة
- 🟢 أندرويد/كروم: زر «إضافة إلى الشاشة الرئيسية» مباشرة.
- 🍏 iOS: تثبيت يدوي بسيط مع تعليمات داخل اللعبة.
- 🕹️ اللعب أوفلاين ضد البوتات بعد أول تحميل.

### (اختياري لاحقاً) حزم للمتاجر
- **Capacitor** أو **TWA (Bubblewrap)** لتوليد APK/AAB من نفس الكود لرفعه على Google Play. يقرر لاحقاً — ليس مطلوباً في الأوفرهول الأول.
- ملف PC القابل للتحميل: GitHub Release يحتوي ZIP (`Play_Game.bat` يعمل بداخله كما اليوم). يُنشر مع كل إصدار.

---

## 📱 المرحلة 3 — إصلاح العرض على الهاتف (Mobile Display Overhaul)

### 3.1 مساحة العرض الصحيحة
- استبدال `100vh` بـ `100dvh` (سطر ~1501 في `style.css` وأي مكان مشابه) لأن شريط عنوان المتصفح المتحرك يقطع `100vh` على الهاتف.
- تفعيل النتش (Notch) والحواف الآمنة عبر:
  ```css
  padding: env(safe-area-inset-top) env(safe-area-inset-right)
           env(safe-area-inset-bottom) env(safe-area-inset-left);
  ```
  على طبقة HUD وأزرار اللمس (الـ viewport يستخدم `viewport-fit=cover` ✅ موجود).

### 3.2 منع سلوكيات المتصفح المزعجة
- `touch-action: none` على `html, body, canvas` (موجود جزئياً على canvas — يُوسَّع).
- قمع القائمة المنبثقة بالضغط المطوّل (`contextmenu` → `preventDefault`) والمكبّر المغناطيسي في iOS.
- منع السحب لتحديث الصفحة (pull-to-refresh): `overscroll-behavior: none`.

### 3.3 الوضع الأفقي (Landscape)
- كشف الوضع العامودي أثناء المباراة → شاشة «🔄 أدر جهازك أفقياً» أنيقة بنمط نيون اللعبة.
- (النشرة المثبتة كـ PWA ستقفل landscape عبر الـ manifest تلقائياً).

### 3.4 الكاميرا والرؤية
- الشاشة الهاتفية أصغر → المشهد يبدو مقصوصاً. تعديل زموم الكاميرا ديناميكياً في `Renderer.js`:
  `zoom = clamp(min(720 / innerWidth, 1.0) * baseZoom, minZoom, maxZoom)` لرؤية أوسع على الهاتف.
- الحفاظ على موضع اهتزاز الشاشة والجسيمات كما هو (تحجيم على العرض فقط).

### 3.5 HUD مضغوط للهاتف
- شريط `hud-top-bar` و`hud-bottom-bar`: تصغير تلقائي، إخفاء FPS/PING داخل زر صغير على الشاشات < 480px.
- أهداف لمس ≥ 48×48px لكل الأزرار (معايير Apple/Google).
- إخفاء شريط الأسلحة الخماسي على الهاتف (زر GUN موجود) وإظهاره كشريط شفاف مختصر تحت الإبهام الأيسر.

### 3.6 إدارة الجلسة
- تفعيل **Fullscreen API** تلقائياً عند بدء المباراة (لمسة المستخدم تسمح بذلك).
- **Wake Lock API** لمنع نوم الشاشة أثناء المباراة (مع تحرير القفل عند النهاية).
- عند خروج المستخدم من التبويب وعودته: إيقاف مؤقت تلقائي + إعادة مزامنة الشبكة.

---

## 🎮 المرحلة 4 — تحسين التحكم على الهاتف (Controls Overhaul)

الأساس موجود في `js/engine/Input.js` (عصيان ديناميكيتان + double-tap + flick gestures). نبني عليه:

### 4.1 تحسين عصا الحركة (يسار)
- مؤشر بصري دائم: حلقة أساس + مقبض داخلي يتبع الإبهام (hexagon نيون).
- Deadzone قابلة للضبط (افتراضي 12–18px) + نصف قطر أقصى موحّد 70px مع «snap» سلس للعودة.
- منع «انزلاق موضع العصا»: تثبيت نقطة الأصل بعد أول 8px سحب ليتوقع اللاعب مكانها.

### 4.2 تحسين عصا التصويب (يمين)
- **خيار إطلاق نار تلقائي أثناء التصويب** (Auto-Fire): رمي أثناء السحب بدلاً من الرمي عند الإفلات فقط — اختياري من الإعدادات (الافتراضي: تلقائي، لأنه أسهل على الهاتف).
- **تعويض مساعدة التصويب (Aim Assist)**: مغنطة خفيفة 4–6° تُدمج زاوية التصويب نحو أقرب عدو داخل مخروط 12° عند التصويب البعيد. ميزة هاتفية تعوّض دقة اللمس ضد الفأرة.
- منع تفعيل الـ PARRY عرضياً عبر «المس الثانوي»: رفع عتبة الضغط المزدوج قليلاً وإظهار وميض تأكيد على الزر.

### 4.3 الأزرار
- إعادة ترتيب الإبهام الطبيعي: `SUPER` فوق `PARRY`، وتجميع `DASH` و`SLIDE` عمودياً بجانب عصا التصويب؛ أزرار `PING`/`RELOAD`/`GUN` أصغر في صف علوي.
- شفافية الأزرار + حجمها = إعدادات جديدة في لوحة الإعدادات الحالية (تُحفظ في `localStorage`).
- **وضع الأعسر (Lefty Mode)**: قلب تخطيط الأزرار 180°.
- **Haptics**: `navigator.vibrate(15)` عند الإصابة/الـ Parry الناجح/اكتمال الـ SUPER (يحترم إعداد قوة الاهتزاز الموجود).

### 4.4 استجابة اللمس
- الانتقال من `touchstart/touchmove` إلى **Pointer Events** الموحّدة (`pointerdown/move/up`) — يقلل التأخير المُقاس ~8–16ms ويوحّد القلم/الموسي أيضاً على الشاشات اللمسية للكمبيوتر.
- تسجيل ضغطات الأزرار بلمسة `pointerdown` فوراً وليس `click` (توفير حتى ~100ms على بعض أندرويد).

### 4.5 إعدادات لمس جديدة (تُضاف للقائمة الموجودة)
- حساسية عصا التصويب (slider).
- Auto-Fire on/off.
- Aim Assist strength: off/low/medium.
- حجم وشفافية أزرار اللمس.
- وضع الأعسر.

---

## ⚡ المرحلة 5 — أداء الهاتف + الصقل النهائي

1. **Dynamic Resolution (FPS Governor):**
   - في `main.js` (حلقة اللعبة): إذا انخفض المتوسط تحت 45fps لمدة ثانيتين → خفض `renderer.setResolutionScale()` بمقدار 0.15 حتى أدنى 0.6؛ إذا استقر فوق 58fps → رفعه تدريجياً حتى 1.0.
2. **ميزانية الجسيمات:** رفع كثافة `low` تلقائياً على الأجهزة ذات `hardwareConcurrency ≤ 4` أو عند 45fps مستمر.
3. **تحميل أسرع:** تقليص/ضغط `peerjs.min.js` المحلي جاهز، إضافة `defer` للسكربتات، `dns-prefetch` لـ `0.peerjs.com`.
4. **اختبار حقيقي — مصفوفة القبول:**
   - 📱 iPhone Safari (13 mini → شاشة صغيرة أسوأ حالة) وAndroid Chrome متوسط (Snapdragon 695 مثلاً).
   - 🧪 Chrome DevTools: محاكاة Moto G Power + CPU 4x slowdown + 3G throttling.
   - 🏆 الهدف: 55–60fps ثابتة أثناء معركة 4 لاعبين على جهاز متوسط.
5. **إصلاحات iOS الشائعة:** التحقق من فتح `AudioContext` بلمسة أولى، ومنع تمدد الشاشة عند ظهور شريط Safari.
6. **تحديث `README.md`**: خطوات النشر على Render + رابط اللعب + تعليمات تثبيت الـ PWA بالعربية.

---

## 🗓️ الترتيب الزمني المقترح (بدون أيام ضاغطة)

| الترتيب | المهمة | الجهد | يعتمد على |
|---|---|---|---|
| 1 | المرحلة 1: `render.yaml` + نشر Static Site | 30 دقيقة + دقائق نشر | — |
| 2 | المرحلة 2: manifest + Service Worker + أيقونات | 2–3 ساعات | المرحلة 1 (لطلب التحقق من الـ HTTPS) |
| 3 | المرحلة 3: عرض الهاتف (dvh, safe-area, rotate overlay, كاميرا) | 4–6 ساعات | — |
| 4 | المرحلة 4: تحسين التحكم (Pointer Events + Auto-Fire + Aim Assist + أزرار) | 6–8 ساعات | المرحلة 3 (تخطيط الأزرار) |
| 5 | المرحلة 5: أداء + اختبار + README | 3–4 ساعات | الكل |

> الرهان الأصغر فيه أكبر مكسب: **المرحلتان 1 و2 تحققان «قابلة للتحميل ومنشورة» خلال ساعات**، والباقي تحسينات تدراكية بدون تغيير بنية اللعبة.

---

## ⚠️ مخاطر وملاحظات تقنية

| الخطر | التخفيف |
|---|---|
| PeerJS cloud المجاني قد يكون محدوداً حملاً (غرف كثيرة متزامنة) | البدء به، ثم استضافة PeerServer خاص على Render (Web Service) إن لزم — اللعبة تشير للوسيط فقط، الكود لا يتغير بنيوياً |
| WebRTC محجوب في بعض الشبكات (جدران نارية شركات/جامعات) | وضع البوتات أوفلاين + رسالة لطيفة «اتصال P2P محظور — العب وضع البوتات» |
| iOS Safari لا يعرض زر تثبيت تلقائي | تعليمات مرئية داخل اللوبي تُعرض فقط على iOS |
| Render Free Web Services تنام (للخيار B فقط) | الاعتماد على Static Site (لا ينام) أو خدمة مجانية أخرى للوسيط إن لزم |
| حفظ حالة اللعبة أوسع من `localStorage` | يبقى كما الآن (لا قاعدة بيانات مطلوبة في هذه الخطة) |

---

## ✅ قائمة القبول النهائية (Definition of Done)

- [ ] رابط Render ثابت يفتح اللعبة من أي شبكة بدون تثبيت أي شيء.
- [ ] من كروم/سامسونج يظهر زر «تثبيت التطبيق» ويُثبّت أيقونة نيون على الشاشة الرئيسية.
- [ ] اللعب أوفلاين ضد البوتات بعد التثبيت.
- [ ] على الهاتف: HUD لا يغطيه شريط المتصفح ولا النوتش، والأزرار تحت الإبهام بدون عناء.
- [ ] تحكم: Deadzone محسوبة، Auto-Fire اختياري، Aim Assist خفيف، اهتزاز haptic.
- [ ] شاشة «أدر جهازك» عند الوضع العامودي + قفل landscape في PWA.
- [ ] 55+ fps على جهاز أندرويد متوسط أثناء معركة كاملة.
- [ ] README محدّث: رابط اللعب + تعليمات التثبيت + خطوات إعادة النشر.
